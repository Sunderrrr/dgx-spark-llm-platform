# Secrets & environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc

# Data volumes
portal_data/
postgres_data/

# Databases & dumps
*.db
*.sqlite
*.sqlite3
*.sql
*.dump

# Local backups / editor cruft
*.bak
*.orig
*~
.DS_Store

# Secrets material (never commit)
*.pem
*.key
*.crt
*.p12
id_rsa*
DEBUG_USERS.txt

# Logs
*.log

# Screenshots that contain real user data (leaderboard is per-user by name)
assets/ranking.png

# Users page screenshot contains internal usernames (like the leaderboard)
assets/users.png

# Configuration SearXNG : contient un secret généré au premier démarrage.
searxng/

# Jetons de service générés localement (crawl4ai). Jamais dans git.
secrets/

# Machine-private security notes (operator-specific; never commit)
.machine/

# Guide d'exploitation (CLAUDE.md) : lu depuis le disque par l'opérateur et les
# agents qui travaillent dans le dépôt, mais il ne se publie pas. Le motif sans
# barre couvre aussi dgx-portal-frontend/CLAUDE.md. À ne pas confondre avec
# dgx-portal-frontend/public/avatars/claude.svg, qui est une image et reste suivi.
CLAUDE.md
